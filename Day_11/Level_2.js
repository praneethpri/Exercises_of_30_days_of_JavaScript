const users = require('./Level_1.js')

// 1) Iterate through the users array and get all the keys of the object using destructuring
let keysOfUsers = x => {
  return (x)
}
console.log(keysOfUsers(users))
